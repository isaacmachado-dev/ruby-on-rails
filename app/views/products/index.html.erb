<%# app/views/products/index.html.erb %>
<h1>Products</h1>
<h2><%= t ".title" %></h2>
<h1><%= t "hello" %></h1>

<%# Só aparece New Product if autenticado(logado) %>
<%= link_to "New Product", new_product_path if authenticated? %>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product_path(product.id) %>
    </div>
  <% end %>
</div>